fig_size:
  w: 15
  h: 15
index_table:
  ndvi:
    min: -1
    max: 1
  kndvi:  # With sigma = 0.5(NIR+red) the range should be 0 to approx 0.762 (tanh(0) to tanh(1))
    min: 0
    max: 0.8
  rvi:
    min: 0
    max: 50
  savi:
    min: -1.5
    max: 1.5
  nbr:
    min: -1
    max: 1
default_dir: '/home/petteri/EODIE/results/'
default_lookup: '/home/petteri/EODIE/results/script_test/lookuptable.txt'
default_out: '/home/petteri/EODIE/pngresults'
default_index: ['ndvi']
default_start: '20200101'
default_end: '20201231'
default_id: ['all']
default_show: '0'
default_cmap: 'viridis'
default_series: '0'
