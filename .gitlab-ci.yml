image: continuumio/miniconda3:latest

before_script:
  - python --version
  - conda env create -f environment.yml
  - conda activate eodie

stages:
  - Test

pytest:
  stage: Test
  script:
  - pytest
