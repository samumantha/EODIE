image: continuumio/miniconda3:latest

before_script:
  - python --version
  - conda env create -f environment.yml
  - source activate eodie

stages:
  - Test

pytest:
  stage: Test
  script:
  - pytest
